# Modern ToDo App 🚀

Java (Spring Boot) + MySQL + Docker で構築したToDoアプリです。

## 機能
- **ドラッグ不要の自動ソート**: 完了したタスクは自動で下へ。
- **直感的な編集**: ダブルクリックでタイトル変更。
- **モダンデザイン**: ダークモード対応UI。

## 起動方法
```bash
cd docker
docker-compose up -d
```
